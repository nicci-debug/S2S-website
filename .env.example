# Supabase project (Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: enables the real AI provider. Without this, the AI generation
# endpoint automatically falls back to the deterministic MockProvider so
# the whole product works with zero external secrets.
ANTHROPIC_API_KEY=
